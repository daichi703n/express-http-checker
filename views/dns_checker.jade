extends layout

block content
  h1= title
  p Welcome to #{title}

  h2 DNS Request :
  form(method="GET", target="./")
    input(type="text", name="path" value="#{resPath}" size="30")
    //input(type="hidden", name="mode" value="input")
    input(type="submit", value="Submit")

  p Path:  #{resPath}
  p A Record:  #{aRecord}
  p PTR Record:  #{revRecord}
  //p Response:  #{resBody}

  p <a href="./">Top</a>
